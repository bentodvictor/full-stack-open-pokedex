name: Hello World!

on:
  push:
    branches:
      - main
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "H3ll0 W0rld!"
      - name: Now it is
        run: date
      - name: Directory content
        run: ls -l
